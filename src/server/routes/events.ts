/**
 * SSE events route — stub for Task 2 compilation.
 * Full implementation completed in Task 3 (sse-bridge).
 */

import type { FastifyInstance } from 'fastify';

export async function eventsRoute(_fastify: FastifyInstance): Promise<void> {
  // Implemented in Task 3
}
